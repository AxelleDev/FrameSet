# Server config
# Origin of the frontend, used for CORS and cookies (e.g. http://localhost:5173).
FRONTEND_ORIGIN=
PORT=3000

# Database configuration (MySQL or MariaDB).
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=frameset_db

# Email / SMTP configuration.
# - In DEVELOPMENT you can leave all of these EMPTY: the app then auto-creates an
#   Ethereal test account on first send and logs a preview URL to the console
#   (nothing is sent to real inboxes). Clone, run, done — no email setup needed.
# - To send real emails, fill these with any SMTP provider (Brevo, Resend, Gmail
#   app password, ...). In PRODUCTION they are REQUIRED.
MAIL_HOST=
MAIL_PORT=
MAIL_SECURE=
MAIL_USER=
MAIL_PASS=

# JWT configuration — use long random strings, e.g.:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
JWT_REFRESH_SECRET=

# Bcrypt salt rounds (cost factor). 12 is a good default.
BCRYPT_SALT_ROUNDS=12

# Logging level (debug | info | warn | error)
LOG_LEVEL=info
